{% extends 'base.html.twig' %}

{% block title %}Editer la dépense{% endblock %}

{% form_theme form 'bootstrap_5_layout.html.twig' %}
{% block body %}
{{include('topbar.html.twig')}}
<div class="col-4 offset-4">
    <h1 class="text-pd-yellow text-center">Editer la prestation</h1>

    {{form_start(form)}}
		{{form_row(form._token)}}
		<div class="row">
			<div class="col">
				{{form_row(form.name)}}
			</div>
		</div>
		<div class="row">
			<div class="col">
				{{form_row(form.outcome_date)}}
			</div>
			<div class="col">
				{{form_row(form.outcome_amount)}}
			</div>
		</div>
		<div class="row">
			<div class="py-3 d-grid">
				<button type="submit" class="btn btn-block btn-success ">Ajouter la prestation</button>
			</div>
		</div>
		{# {{form_row(form.Envoyer)}} #}
	</div>
	{{form_end(form)}}
</div>
    <div class="d-flex col-2 offset-5 justify-content-around">
        <a class="btn btn-primary" href="{{ path('app_outcome_index') }}">Retour</a>
        {{ include('outcome/_delete_form.html.twig') }}
    </div>
    {{include('botbar.html.twig')}}
{% endblock %}

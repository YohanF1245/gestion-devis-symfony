{% extends 'base.html.twig' %}

{% block title %}EstimatePerformanceLink{% endblock %}

{% block body %}
    <h1>EstimatePerformanceLink</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estimate_performance_link.id }}</td>
            </tr>
            <tr>
                <th>Estimate_tab_id</th>
                <td>{{ estimate_performance_link.estimateTabId }}</td>
            </tr>
            <tr>
                <th>Performance_id</th>
                <td>{{ estimate_performance_link.performanceId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_estimate_performance_link_index') }}">back to list</a>

    <a href="{{ path('app_estimate_performance_link_edit', {'id': estimate_performance_link.id}) }}">edit</a>

    {{ include('estimate_performance_link/_delete_form.html.twig') }}
{% endblock %}

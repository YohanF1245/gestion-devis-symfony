	<label class="form-label">Choisir le devis a éditer en facture</label>
<select class="form-select" id="selectEstimate" name="estimateSelect" aria-label="" 
{% if app.request.attributes.get('_route') == 'app_facture_emit_new' %}
	required
{% endif %}
>
	<option disabled selected value>Selectionner le devis</option>
	{% if estimates is not empty %}
	{% for estimate in estimates %}
		<option value='{{estimate.id}}'>
			N° {{estimate.estimateNumber}} -
			{{estimate.intitule}} -
			{{estimate.total}} €
		</option>
	{# {% for i in 1..estimates|length %}
		<option value='{{estimates[i].id}}'>
			N° {{estimates[i].estimateNumber}} -
			{{estimates[i].intitule}} -
			{{estimates[i].total}} €
		</option> #}
	{% endfor %}
	{% endif %}
</select>
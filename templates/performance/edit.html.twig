{% extends 'base.html.twig' %}

{% block title %}Editer la prestation{% endblock %}

{% form_theme form 'bootstrap_5_layout.html.twig' %}
{% block body %}
{% include 'navbar.html.twig' %}
<div class="col-4 offset-4 text-white">
    <h1 class="text-pd-yellow text-center">Editer la prestation</h1>
     {{form_start(form)}}
     <div class="row">
        <div class="col">
            {{form_row(form.designation)}}
        </div>
        <div class="col">
            {{form_row(form.quantity)}}
        </div>
    </div>
    <div class="row">
        <div class="col">
            {{form_row(form.unit)}}
        </div>
        <div class="col">
            {{form_row(form.pirce)}}
        </div>
    </div>
    <div class="row">
    <div classs="row">
        {{form_row(form.tax)}}
    </div>
    </div>
    <div class="row">
        <div class="py-3 d-grid">
            <button type="submit" class="btn btn-block btn-success ">Editer la prestation</button>
        </div>
        {# {{form_row(form.Envoyer)}} #}
    </div>
    {{form_end(form)}}
    </div>

    
</div>
    <div class="d-flex col-2 offset-5 justify-content-around">
        <a class="btn btn-primary" href="{{ path('app_performance_index') }}">Retour</a>
        {{ include('performance/_delete_form.html.twig') }}
    </div>
{% endblock %}

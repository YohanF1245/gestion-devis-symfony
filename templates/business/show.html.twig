{% extends 'base.html.twig' %}

{% block title %}Business{% endblock %}

{% block body %}
    <h1>Business</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom </th>
                <td>{{ business.name}}</td>
            </tr>
            <tr>
                <th>Siret</th>
                <td>{{ business.siret }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ business.logo }}</td>
            </tr>
            <tr>
                <th>Code Ape</th>
                <td>{{ business.code_ape }}</td>
            </tr>
            <tr>
                <th>Code TVA</th>
                <td>{{ business.code_tva }}</td>
            </tr>
            <tr>
                <th>RIB</th>
                <td>{{ business.rib }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_business_index') }}">back to list</a>

    <a href="{{ path('app_business_edit', {'id': business.id}) }}">edit</a>

    {{ include('business/_delete_form.html.twig') }}
{% endblock %}
